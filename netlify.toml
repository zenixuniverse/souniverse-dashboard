[build]
  command = "npm run build"
  publish = ".next"

# Enable serverless functions for API routes
[functions]
  directory = ".netlify/functions"
  node_bundler = "esbuild"

# Don't need redirects for Next.js (handles routing internally)
